{
  "name": "frida-ios",
  "version": "1.0.0",
  "description": "基于 frida-compile 的 ios 逆向项目工程",
  "main": "src/index.js",
  "scripts": {
    "build": "frida-compile src/index.js -o dist/hook.js -c",
    "watch": "frida-compile src/index.js -o dist/hook.js -w",
    "start": "npm run build && frida -U -f $bundle_id -l ./dist/hook.js"
  },
  "keywords": [],
  "author": "NeroXie",
  "license": "ISC",
  "devDependencies": {
    "@types/frida-gum": "^17.2.0",
    "@types/node": "^16.11.12",
    "frida-compile": "^10.2.5",
    "source-map": "^0.7.4"
  }
}